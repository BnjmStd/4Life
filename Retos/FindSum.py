# backtrcking
def find_sum(numeros:list, target:int) -> list:
    

    def backtracking(combi:list):
        # solución encontrada
        return

        # no posee solución
        if target < 0 or start == len(numbers):

        #backtrack
        return
    
    result = []
    numeros.sort()
    backtracking([])
    return result


print(find_sum([1, 5, 3, 2], 6))